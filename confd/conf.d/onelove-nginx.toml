[template]
prefix = "/onelove"
src = "nginx.tmpl"
dest = "/etc/nginx/conf.d/onelove.conf"
owner = "nginx"
mode = "0644"
keys = [
  "/upstream",
]
check_cmd = "/usr/sbin/nginx -t"
reload_cmd = "/usr/sbin/service nginx reload"
